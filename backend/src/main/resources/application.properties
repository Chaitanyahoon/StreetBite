server.port=${PORT:${SERVER_PORT:8080}}
spring.application.name=streetbite-backend

# Geocoding is optional. Leave blank to disable remote geocoding calls.
google.geocoding.api.key=${GOOGLE_GEOCODING_API_KEY:${GOOGLE_MAPS_API_KEY:}}

# Firebase is optional and only used for realtime mirrors and push messaging.
firebase.credentials.path=${GOOGLE_APPLICATION_CREDENTIALS:./firebase-key.json}

# Cross-origin and cookie behavior for local development and Render/Vercel deployments.
cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001,http://localhost:4000}

# MySQL source-of-truth database.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/streetbite?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.sql.init.mode=${SPRING_SQL_INIT_MODE:always}

spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:true}
# Validate schema by default in non-development deployments to avoid automatic production schema changes.
# Set SPRING_JPA_HIBERNATE_DDL_AUTO=update explicitly in local/dev if you want auto-migration during development.
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:validate}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.servlet.multipart.max-file-size=${SPRING_SERVLET_MULTIPART_MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${SPRING_SERVLET_MULTIPART_MAX_REQUEST_SIZE:10MB}

# Mail is optional. If unset, password-reset links are logged instead of sent.
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:}
spring.mail.password=${SPRING_MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_SMTP_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.starttls.required=${SPRING_MAIL_SMTP_STARTTLS_REQUIRED:true}
spring.mail.properties.mail.smtp.connectiontimeout=${SPRING_MAIL_SMTP_CONNECTION_TIMEOUT:10000}
spring.mail.properties.mail.smtp.timeout=${SPRING_MAIL_SMTP_TIMEOUT:10000}
spring.mail.properties.mail.smtp.writetimeout=${SPRING_MAIL_SMTP_WRITE_TIMEOUT:10000}
spring.mail.properties.mail.smtp.ssl.trust=${SPRING_MAIL_SMTP_SSL_TRUST:smtp.gmail.com}

logging.level.com.streetbite=${LOG_LEVEL_APP:INFO}
logging.level.org.springframework.web=${LOG_LEVEL_WEB:INFO}
logging.level.org.springframework.transaction=${LOG_LEVEL_TX:INFO}
logging.level.org.springframework.orm.jpa=${LOG_LEVEL_JPA:INFO}
logging.level.org.hibernate.resource.transaction=${LOG_LEVEL_HIBERNATE_TX:INFO}
